/**
 * Created by Ivan on 8/7/2016.
 * Work in progress
 */

/*function createChart(type) {
    var currentType = type.toString();
    require(['../scripts/libs/chartjs/chart.js'], function (Chart) {
        var $canvasContainer=$('#char-container').html('');  //clears the previous canvas
        $('<canvas id="myChart" width="400" height="250" />').appendTo($canvasContainer); //creates a new canvas
        let ctx = document.getElementById("myChart"),
            myChart = new Chart(ctx, {
                type: currentType,
                options: {
                    scales: {
                        yAxes: [{
                            ticks: {
                                beginAtZero: true
                            }
                        }]
                    }
                }
            });

    });

}
createChart('box');*/
